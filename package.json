{
  "name": "my-photos-compare",
  "version": "1.0.0",
  "description": "Download all the files from one Google Photos account, which are not present in another account. This is useful when you have two Google Photos accounts and you want to merge them.",
  "scripts": {
    "start": "ts-node src/index.ts"
  },
  "dependencies": {
    "google-auth-library": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
